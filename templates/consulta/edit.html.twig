<!DOCTYPE html>

<title>Edit Consulta</title>

{% block body %}
    <h1>Edit Consulta</h1>

    {{ include('consulta/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('consulta_index') }}">back to list</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
