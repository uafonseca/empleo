<!DOCTYPE html>

<title>Consulta</title>

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultum.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ consultum.createdAt ? consultum.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ consultum.type }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ consultum.texto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('consulta_index') }}">back to list</a>

    <a href="{{ path('consulta_edit', {'id': consultum.id}) }}">edit</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
