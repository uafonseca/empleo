{% extends 'backend/base.html.twig' %}

{% block title %}Edit StaticPage{% endblock %}

{% block context %}
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <div class="row">
                <h6 class="m-0 font-weight-bold text-primary col-6">Nueva</h6>
            </div>
        </div>
        <div class="card-body">
            {{ include('backend/static_page/_form.html.twig') }}

            <a href="{{ path('static_page_index') }}" class="btn btn-secondary mt-3 ml-2"><i class="fas fa-arrow-left"></i> Regresar</a>

           <div class="ml-2">
               {{ include('backend/static_page/_delete_form.html.twig') }}
           </div>
        </div>
    </div>

{% endblock %}
