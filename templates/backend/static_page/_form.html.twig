{{ form_start(form) }}
    <div class="col-6">
        {{ form_row(form.type) }}
    </div>
    <div class="col-12">
        {{ form_row(form.context) }}
    </div>
    <div class="col-12">
        <button class="btn btn-success"><i class="fas fa-save"></i> {{ button_label|default('Guardar') }}</button>
    </div>
{{ form_end(form) }}
