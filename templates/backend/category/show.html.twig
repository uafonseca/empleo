{% extends 'backend/base.html.twig' %}

{% block title %}Category{% endblock %}

{% block context %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Icono</th>
                <td>{{ category.ico }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ category.description }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('category_index') }}" class="btn btn-secondary btn-icon-split">
                    <span class="icon text-white-50">
                      <i class="fas fa-arrow-left"></i>
                    </span>
        <span class="text">Regresar</span>
    </a>
    <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-warning btn-icon-split">
                    <span class="icon text-white-50">
                      <i class="fas fa-edit"></i>
                    </span>
        <span class="text">Editar</span>
    </a>



    {{ include('backend/category/_delete_form.html.twig') }}
{% endblock %}
