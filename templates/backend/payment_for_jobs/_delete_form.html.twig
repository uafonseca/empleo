<form method="post" action="{{ path('payment_for_jobs_delete', {'id': payment_for_job.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ payment_for_job.id) }}">
    <br>
    <button class="btn btn-danger"> <i class="fa fa-trash"></i> Eliminar</button>
</form>
