<form method="post" action="{{ path('respuesta_consulta_delete', {'id': respuesta_consultum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ respuesta_consultum.id) }}">
    <button class="btn">Delete</button>
</form>
