{% extends 'mail/base.html.twig' %}
{% block content %}
	<div class="container">
		<img src="{{ absolute_url(asset('build/backend/images/logo_last.png')) }}" alt="Emplear_Logo"/>
		<h2>
			Se ha publicado una oferta de trabajo que quizás te interese
		</h2>
		<h3>{{job.title}}</h3>
		<br/>
		{% if job.id is not null %}
			<a class="button" href="{{ absolute_url(path('job_show',{id:job.id})) }}">Ver empleo</a>
		{% endif %}
		<br/>
		<br/>
	</div>
{% endblock %}
