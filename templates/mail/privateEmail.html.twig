{% extends 'mail/base.html.twig' %}
 {% block content %}
     <div class="container">
         <div class="row">
             <div class="col-md-offset-3"></div>
             <div class="col-md-6">
                 <img src="{{ absolute_url(asset('site/images/logo.png')) }}" alt="Emplear_Logo">
             </div>
             <div class="col-md-offset-3"></div>
         </div>
         <div class="row">
            <div class="col-sm-12">
               <p>Nuevo mensaje de {{ message.creator.name }}</p>
                <p>{{ message.context|raw }}</p>
            </div>
         </div>
     </div>
 {% endblock %}