{% extends 'mail/base.html.twig' %}
 {% block content %}
     <div class="container">
         <div class="row">
             <div class="col-md-offset-3"></div>
             <div class="col-md-6">
                 <img src="{{ absolute_url(asset('site/images/logo.png')) }}" alt="Emplear_Logo">
             </div>
             <div class="col-md-offset-3"></div>
         </div>
         <div class="row">
            <div class="col-sm-12">
                <hr>
                <h2>Notificación</h2>
                <p>{{ notification.context|raw }}</p>
            </div>
         </div>
     </div>
 {% endblock %}