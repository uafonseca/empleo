<!DOCTYPE html>

<title>Slide</title>

{% block body %}
    <h1>Slide</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slide.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ slide.title }}</td>
            </tr>
            <tr>
                <th>ShortDescription</th>
                <td>{{ slide.shortDescription }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ slide.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('slide_index') }}">back to list</a>

    <a href="{{ path('slide_edit', {'id': slide.id}) }}">edit</a>

    {{ include('slide/_delete_form.html.twig') }}
{% endblock %}
